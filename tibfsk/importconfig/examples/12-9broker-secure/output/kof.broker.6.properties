# ====================================================================
# KOF-CONFIG-STATUS: ACCEPTED
# Settings in this file were automatically converted by the tool tibftlimportconfig.  All
# settings after conversions are compatible with FSK.  Settings that could not
# automatically convert were written to a separate file: unsupported.properties.
# ====================================================================

# TIBCO FTL(R) Service for Kafka Server Properties
# Generated by conversion tool from: server-6.properties
# Node ID: 6

# --- Listeners ---
listeners=OAUTH://localhost:9144,MTLS://localhost:9145
advertised.listeners=OAUTH://localhost:9144,MTLS://localhost:9145
listener.security.protocol.map=OAUTH:SASL_SSL,MTLS:SSL
listener.name.basic_auth.sasl.enabled.mechanisms=PLAIN
listener.name.oauth.sasl.enabled.mechanisms=OAUTHBEARER
listener.name.oauth.oauthbearer.connections.max.reauth.ms=3600000
listener.name.mtls.ssl.client.auth=required
listener.name.mtls.ssl.keystore.location=/etc/kafka/certs/broker-6.keystore.jks
listener.name.mtls.ssl.truststore.location=/etc/kafka/certs/kafka.ca.truststore.jks

# --- All remaining properties ---
node.id=6
socket.request.max.bytes=104857600
# [source:25] original: ssl.keystore.type=JKS
# FSK reads PEM, not a JKS keystore, so this and ssl.keystore.location were rewritten below.
# Creating the .pem is a real conversion, NOT a rename. Run it before starting
# tibftlserver (or re-run tibftlimportconfig with --auto to run it for you):
#   keytool -importkeystore -srckeystore /etc/kafka/certs/broker-6.keystore.jks -srcstoretype JKS \
#           -destkeystore /etc/kafka/certs/broker-6.keystore.p12 -deststoretype PKCS12
#   openssl pkcs12 -in /etc/kafka/certs/broker-6.keystore.p12 -nodes -out /etc/kafka/certs/broker-6.keystore.pem
ssl.keystore.type=PEM
# [source:26] original: ssl.keystore.location=/etc/kafka/certs/broker-6.keystore.jks (repointed at the PEM form; see ssl.keystore.type above)
ssl.keystore.location=/etc/kafka/certs/broker-6.keystore.pem
ssl.key.password=keyPassword123
# [source:29] original: ssl.truststore.type=JKS
# FSK reads PEM, not a JKS truststore, so this and ssl.truststore.location were rewritten below.
# Creating the .pem is a real conversion, NOT a rename. Run it before starting
# tibftlserver (or re-run tibftlimportconfig with --auto to run it for you):
#   keytool -importkeystore -srckeystore /etc/kafka/certs/kafka.truststore.jks -srcstoretype JKS \
#           -destkeystore /etc/kafka/certs/kafka.truststore.p12 -deststoretype PKCS12
#   openssl pkcs12 -in /etc/kafka/certs/kafka.truststore.p12 -nodes -nokeys -out /etc/kafka/certs/kafka.truststore.pem
ssl.truststore.type=PEM
# [source:30] original: ssl.truststore.location=/etc/kafka/certs/kafka.truststore.jks (repointed at the PEM form; see ssl.truststore.type above)
ssl.truststore.location=/etc/kafka/certs/kafka.truststore.pem
ssl.enabled.protocols=TLSv1.3,TLSv1.2
num.partitions=10
log.retention.hours=168
