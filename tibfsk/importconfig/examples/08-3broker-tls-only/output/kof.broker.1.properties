# ====================================================================
# KOF-CONFIG-STATUS: ACCEPTED
# Settings in this file were automatically converted by the tool tibftlimportconfig.  All
# settings after conversions are compatible with FSK.  Settings that could not
# automatically convert were written to a separate file: unsupported.properties.
# ====================================================================

# TIBCO FTL(R) Service for Kafka Server Properties
# Generated by conversion tool from: server-1.properties
# Node ID: 1

# --- Listeners ---
listeners=SSL://localhost:9092
advertised.listeners=SSL://localhost:9092
listener.security.protocol.map=SSL:SSL
listener.name.controller.ssl.keystore.location=/etc/kafka/certs/broker-1.keystore.jks
listener.name.controller.ssl.truststore.location=/etc/kafka/certs/kafka.truststore.jks

# --- All remaining properties ---
node.id=1
socket.request.max.bytes=104857600
# [source:44] original: ssl.keystore.type=JKS
# FSK reads PEM, not a JKS keystore, so this and ssl.keystore.location were rewritten below.
# Creating the .pem is a real conversion, NOT a rename. Run it before starting
# tibftlserver:
#   keytool -importkeystore -srckeystore /etc/kafka/certs/broker-1.keystore.jks -srcstoretype JKS \
#           -destkeystore /etc/kafka/certs/broker-1.keystore.p12 -deststoretype PKCS12
#   openssl pkcs12 -in /etc/kafka/certs/broker-1.keystore.p12 -nodes -out /etc/kafka/certs/broker-1.keystore.pem
ssl.keystore.type=PEM
# [source:45] original: ssl.keystore.location=/etc/kafka/certs/broker-1.keystore.jks (repointed at the PEM form; see ssl.keystore.type above)
ssl.keystore.location=/etc/kafka/certs/broker-1.keystore.pem
ssl.key.password=keyPassword123
# [source:48] original: ssl.truststore.type=JKS
# FSK reads PEM, not a JKS truststore, so this and ssl.truststore.location were rewritten below.
# Creating the .pem is a real conversion, NOT a rename. Run it before starting
# tibftlserver:
#   keytool -importkeystore -srckeystore /etc/kafka/certs/kafka.truststore.jks -srcstoretype JKS \
#           -destkeystore /etc/kafka/certs/kafka.truststore.p12 -deststoretype PKCS12
#   openssl pkcs12 -in /etc/kafka/certs/kafka.truststore.p12 -nodes -nokeys -out /etc/kafka/certs/kafka.truststore.pem
ssl.truststore.type=PEM
# [source:49] original: ssl.truststore.location=/etc/kafka/certs/kafka.truststore.jks (repointed at the PEM form; see ssl.truststore.type above)
ssl.truststore.location=/etc/kafka/certs/kafka.truststore.pem
ssl.enabled.protocols=TLSv1.3,TLSv1.2
ssl.client.auth=none
num.partitions=3
log.retention.hours=168
