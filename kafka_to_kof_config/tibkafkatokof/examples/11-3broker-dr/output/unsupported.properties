# unsupported.properties — generated by tibkafkatokof
# These properties are not in the KoF broker properties whitelist and are
# not written to kof.broker.N.properties. They are kept here for reference.
# Section 3 (KRaft cluster-control) keys are always rejected; security-domain
# keys not in the section 1 whitelist are also excluded.

inter.broker.listener.name=PLAINTEXT
controller.listener.names=CONTROLLER
process.roles=broker,controller
controller.quorum.bootstrap.servers=localhost:9093,localhost:9103,localhost:9113
num.network.threads=3
num.io.threads=8
socket.send.buffer.bytes=102400
socket.receive.buffer.bytes=102400
log.dirs=/var/kafka/data/broker-1
num.recovery.threads.per.data.dir=1
offsets.topic.replication.factor=3
share.coordinator.state.topic.replication.factor=3
share.coordinator.state.topic.min.isr=2
transaction.state.log.replication.factor=3
transaction.state.log.min.isr=2
log.segment.bytes=1073741824
log.retention.check.interval.ms=300000
