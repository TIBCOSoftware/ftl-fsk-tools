# ====================================================================
# KOF-CONFIG-STATUS: ACCEPTED
#
# Settings in this file were unable to be automatically converted by the tool tibkafkatokof.  No
# compatible KoF settings were found to be equivalent. Not all unsupported settings will have a
# material impact, and some may be unnecessary.  Unsupported settings are provided here for
# reference, and may be manually resolved by the user if necessary.
# ====================================================================

controller.listener.names=CONTROLLER
process.roles=broker,controller
controller.quorum.bootstrap.servers=localhost:9093
num.network.threads=3
num.io.threads=8
socket.send.buffer.bytes=102400
socket.receive.buffer.bytes=102400
log.dirs=/var/kafka/data/broker-1
num.recovery.threads.per.data.dir=1
offsets.topic.replication.factor=1
share.coordinator.state.topic.replication.factor=1
share.coordinator.state.topic.min.isr=1
transaction.state.log.replication.factor=1
transaction.state.log.min.isr=1
log.segment.bytes=1073741824
log.retention.check.interval.ms=300000
