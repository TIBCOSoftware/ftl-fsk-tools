# ====================================================================
# KOF-CONFIG-STATUS: ACCEPTED
# Nothing is left to resolve. Every security setting is a value KoF understands
# and its params are present; settings that don't apply in KoF are kept with an
# IGNORED note. This is not a guarantee the config works -- only that the tool
# has nothing more to flag.
# ====================================================================

# KOF Broker Properties
# Generated by tibkafkatokof from: testdata/oauth-resolved.properties
# Node ID: 1

# --- Listeners ---
listeners=OAUTH://0.0.0.0:9094
listener.security.protocol.map=OAUTH:SASL_PLAINTEXT
listener.name.oauth.sasl.enabled.mechanisms=OAUTHBEARER
# [source:5] original: listener.name.oauth.oauthbearer.sasl.server.callback.handler.class=org.apache.kafka.common.security.oauthbearer.OAuthBearerValidatorCallbackHandler
listener.name.oauth.oauthbearer.sasl.server.callback.handler.class=oauth
listener.name.oauth.oauthbearer.sasl.oauthbearer.jwks.endpoint.url=https://keycloak/realms/r/protocol/openid-connect/certs
listener.name.oauth.oauthbearer.sasl.oauthbearer.expected.issuer=https://keycloak/realms/r

# --- All remaining properties ---
node.id=1
