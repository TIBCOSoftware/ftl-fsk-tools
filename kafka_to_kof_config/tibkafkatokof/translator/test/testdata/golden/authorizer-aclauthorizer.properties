# ====================================================================
# KOF-CONFIG-STATUS: ACCEPTED
# Nothing is left to resolve. Every security setting is a value KoF understands
# and its params are present. Properties not in the KoF whitelist were written
# to unsupported.properties. This is not a guarantee the config works -- only
# that the tool has nothing more to flag.
# ====================================================================

# KOF Broker Properties
# Generated by tibkafkatokof from: testdata/authorizer-aclauthorizer.properties
# Node ID: 1

# --- Listeners ---
listeners=PLAINTEXT://0.0.0.0:9092

# --- All remaining properties ---
node.id=1
# [source:3] original: authorizer.class.name=kafka.security.authorizer.AclAuthorizer
# kafka.security.authorizer.AclAuthorizer rewritten to "standard". KoF enforces the same ACL model:
# default-deny, super.users bypass, per-principal allow rules.
authorizer.class.name=standard
