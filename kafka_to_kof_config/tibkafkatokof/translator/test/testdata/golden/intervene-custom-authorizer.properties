# ====================================================================
# KOF-CONFIG-STATUS: INVALID
# This file has UNRESOLVED settings and MUST NOT be run. Each block tagged
# RESOLVE-REQUIRED below needs an edit (a custom Java class to replace with a
# backend, a Java keystore to convert to PEM, or a backend's missing params).
# Jump to each by searching for >>>>>>> , make the edit, and re-run the tool;
# it re-checks and stamps ACCEPTED once nothing is left. Do not edit this
# status line by hand -- the tool sets it.
# ====================================================================

# KOF Broker Properties
# Generated by tibkafkatokof from: testdata/intervene-custom-authorizer.properties
# Node ID: 1

# --- Listeners ---
listeners=PLAINTEXT://0.0.0.0:9092

# --- All remaining properties ---
node.id=1
# >>>>>>>>>>>>>>>> KOF RESOLVE-REQUIRED (edit below) >>>>>>>>>>>>>>>>
# [source:3] original: authorizer.class.name=com.acme.security.MyCustomAuthorizer
# RESOLVE-REQUIRED: unrecognized authorizer. Supported: kafka.security.auth.SimpleAclAuthorizer, kafka.security.authorizer.AclAuthorizer, org.apache.kafka.metadata.authorizer.StandardAuthorizer.
# A custom Java authorizer cannot be run. Uncomment only if it is
# ACL-equivalent to a standard authorizer:
#authorizer.class.name=standard
# <<<<<<<<<<<<<<<< end KOF RESOLVE-REQUIRED <<<<<<<<<<<<<<<<<<<<<<<
