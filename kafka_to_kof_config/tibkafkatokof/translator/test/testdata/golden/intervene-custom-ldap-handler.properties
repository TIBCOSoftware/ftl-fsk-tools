# ====================================================================
# KOF-CONFIG-STATUS: INVALID
# This file has UNRESOLVED settings and MUST NOT be run. Each block tagged
# RESOLVE-REQUIRED below needs an edit (a custom Java class to replace with a
# backend, a Java keystore to convert to PEM, or a backend's missing params).
# Jump to each by searching for >>>>>>> , make the edit, and re-run the tool;
# it re-checks and stamps ACCEPTED once nothing is left. Do not edit this
# status line by hand -- the tool sets it.
# ====================================================================

# KOF Broker Properties
# Generated by tibkafkatokof from: testdata/intervene-custom-ldap-handler.properties
# Node ID: 1

# --- Listeners ---
listeners=SASL://0.0.0.0:9092
listener.security.protocol.map=SASL:SASL_PLAINTEXT
# >>>>>>>>>>>>>>>> KOF RESOLVE-REQUIRED (edit below) >>>>>>>>>>>>>>>>
# [source:5] original: listener.name.sasl.plain.sasl.server.callback.handler.class=com.acme.security.LdapPlainCallbackHandler
# RESOLVE-REQUIRED: KoF cannot run custom Java handler class.
# Name suggests "ldap" (one of ldap, oauth, file, inline) -- verify, then uncomment:
#listener.name.sasl.plain.sasl.server.callback.handler.class=ldap
# configure an ldap provider in the realm (auth.providers=ldap:...)
# <<<<<<<<<<<<<<<< end KOF RESOLVE-REQUIRED <<<<<<<<<<<<<<<<<<<<<<<
listener.name.sasl.plain.sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required;

# --- All remaining properties ---
node.id=1
sasl.enabled.mechanisms=PLAIN
